<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ahmed.jms">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Login Job Seakeer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainLoginActivity"
            android:label="Registeration Job Seaker"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainUserActivity"
            android:label="Welcome To JMS"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainCVActivity"
            android:label="@string/title_activity_main_cv" />
        <activity
            android:name=".MainLoginHRActivity"
            android:label="Login HR"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainHRActivity"
            android:label="@string/title_activity_main_hr"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".JopsActivity" />
        <activity
            android:name=".MainJobDetails"
            android:label="@string/title_activity_main_job_details"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsJobActivity"
            android:label="@string/title_activity_maps_job" />
        <activity android:name=".MainJobDetailsActivity" />
        <activity
            android:name=".MainHRManagerActivity"
            android:label="@string/title_activity_main_hrmanager" />
        <activity android:name=".MainRatingActivity" />
        <activity android:name=".ApprovedActivity" />
        <activity android:name=".NotificationActivity" />
        <activity android:name=".Evaluation2Activity" />
        <activity android:name=".ListEvaluation2" />
        <activity
            android:name=".ConfirmInterviewActivity"
            android:label="Confirmation Contract" />
        <activity android:name=".ReviewCompanyActivity" />
        <activity android:name=".Map"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>